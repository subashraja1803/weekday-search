export const SEARCH_JOBS_ACTIONS = {
  SET_CARDS_DATA: "SEARCH_JOBS_SET_CARD_DATA",
  APPEND_CARDS_DATA: "SEARCH_JOBS_APPEND_CARD_DATA",
  SET_FILTERS: "SEARCH_JOBS_SET_FILTERS",
  SET_OFFSET: "SEARCH_JOBS_SET_OFFSET",
};

// SEARCH_JOBS is added before the values because if there are any other store and similar actions, it would be clear for redux to distinguhs the actions
